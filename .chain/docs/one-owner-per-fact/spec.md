Implement this task on docs/one-owner-per-fact directly from the feature brief below — a direct chain carries no spec or plan phase, so the brief is the specification of record. The platform materializes `.chain/docs/one-owner-per-fact/spec.md` as the specification of record; leave it untouched. The platform pins native child threads to Luna max and limits the session to eight concurrent children. Use them only when the brief contains independent, safely parallel work; group related change points instead of creating one child per item. In the controlled resource limit, fill as many slots as can execute safely in parallel. Give every concurrent writer its own branch and git worktree, and keep coupled work in your own context. When at least two child-writer branches need integration, start one long-lived merger after the first result is ready; integrate a sole child-writer branch yourself. The merger integrates completed branches in dependency-safe order, resolves only mechanical conflicts, reruns affected narrow tests, and reports semantic conflicts to you. Follow the platform-pinned Implementation proof boundary after integration. Give a failed child one bounded correction in the same thread, then take over its assignment yourself. A child must not perform irreversible external actions. Commit the result and persist exactly one JSON object as the task output: `{"schemaVersion":1,"headSha":"<final HEAD>","baseSha":"<starting HEAD>","summary":"<what changed>","testsRun":["<command>"]}`. Leave publication and pull-request creation to the platform. Complete when the brief's behavior is demonstrably delivered and tests are green at the recorded head.
<!-- agentos:task-brief:v1 length=7300 -->
Implement this task on docs/one-owner-per-fact directly from the feature brief below — a direct chain carries no spec or plan phase, so the brief is the specification of record. The platform materializes `.chain/docs/one-owner-per-fact/spec.md` as the specification of record; leave it untouched. The platform pins native child threads to Luna max and limits the session to eight concurrent children. Use them only when the brief contains independent, safely parallel work; group related change points instead of creating one child per item. In the controlled resource limit, fill as many slots as can execute safely in parallel. Give every concurrent writer its own branch and git worktree, and keep coupled work in your own context. When at least two child-writer branches need integration, start one long-lived merger after the first result is ready; integrate a sole child-writer branch yourself. The merger integrates completed branches in dependency-safe order, resolves only mechanical conflicts, reruns affected narrow tests, and reports semantic conflicts to you. Follow the platform-pinned Implementation proof boundary after integration. Give a failed child one bounded correction in the same thread, then take over its assignment yourself. A child must not perform irreversible external actions. Commit the result and persist exactly one JSON object as the task output: `{"schemaVersion":1,"headSha":"<final HEAD>","baseSha":"<starting HEAD>","summary":"<what changed>","testsRun":["<command>"]}`. Leave publication and pull-request creation to the platform. Complete when the brief's behavior is demonstrably delivered and tests are green at the recorded head.
<!-- agentos:task-brief:v1 length=4459 -->
## Goal

Each of the five documentation facts below is stated by exactly one document that owns it, and the other copies point at the owner instead of restating it.

## Background

Survey candidates SIM-DOCS-001, -002, -003, -005, -006, operator ruling 2026-09-06: repair each by ID rather than delete. Depends on: the auto-deploy runbook chain "Docs: the auto-deploy runbook presents Linux systemd as the production path" — this chain is bound after it so the two do not rewrite `docs/` concurrently.

Facts today: (DOCS-002) the implementation `Route:` line grammar and escalation rule are stated in `docs/governance/task-routing-v1.md` (~80-100), `docs/BRIEF-TEMPLATE.md` (Routing section) and `docs/operator-api.md` (~1104), and the three disagree; the parser's real grammar in `packages/api/src/templates.ts` (~73-97) is `Route: implementation=<agent>` with an optional ` - <reason>` suffix, and a near miss refuses instantiation. (DOCS-003) `docs/install.md` "Start locally" restates the sequence it declares non-authoritative (`docs/release/developer-preview.md`). (DOCS-005) `docs/architecture.md` carries an OSS-C blockquote about screenshots that are not in that file, plus a third copy of the security limits owned by `docs/release/security.md`. (DOCS-001) `docs/demos/templates-release-evidence.schema.json` requires exactly 10 positions while `scripts/templates-release-demo.mjs` emits 12, and has a wrong `$id` host. (DOCS-006) `CONTEXT.md` is a glossary nothing points at, and its "Disposition" and "Blind review" entries name steps that do not exist in the canonical templates.

## Changes

1a. DOCS-002 (escalation rule, operator ruling 2026-09-06): the single escalation section in `docs/governance/task-routing-v1.md` reads, in substance: the implementation step runs on the template default `senior-dev-luna-max`; escalate on **hazard**, never on path. Write `Route: implementation=senior-dev-astra-medium - <hazard>` only when the acceptance suite cannot witness the failure (concurrency, transaction boundaries, lock or lease windows, cross-module contract migrations) or when the change alters what the merge gate, merge automation or a migration does. A brief with mechanical acceptance stays on the default whatever directory it touches; deleting dead code is never a hazard. Delete the path-category rule ("persisted data, a defense-list path ... or uncertain classification") and every sentence in `docs/BRIEF-TEMPLATE.md` or `docs/operator-api.md` that escalates by directory or by a reviewer's recommendation.
1b. The survey skill `.agents/skills/agentos-find-simplifications` (`SKILL.md` ~88 and ~100, `references/candidate-report.md` ~31 and ~57, `agents/openai.yaml` ~4) states routing in the same two words as the owner section: `default route` and `hazard: <which>`; the retired vocabulary `Luna Max eligible` / `Sol High required` is gone.
1. DOCS-002: `docs/governance/task-routing-v1.md` owns the full `Route:` grammar (line shape, optional ` - <reason>` with the exact separator, near-miss refusal code, `implementation_route_template_unsupported`, mutual exclusion with `stepOverrides`) and the escalation criteria in one section. `docs/BRIEF-TEMPLATE.md` Routing keeps the five routes and their reasons but replaces its grammar and escalation prose with a pointer to that section; `docs/operator-api.md` keeps only the refusal codes the route returns and points at the same section.
2. DOCS-003: `docs/install.md` "Start locally" becomes the pointer plus what install.md uniquely owns; the prerequisite table and steps stay only in `docs/release/developer-preview.md`. Preserve every string `scripts/release-docs.test.mjs` pins on install.md (`--existing`, the canonical-name management rule, the add-a-project.md path, the absence of the three canonical-sync commands).
3. DOCS-005: delete the orphaned OSS-C blockquote from `docs/architecture.md`; reduce its security list to the architecture-specific facts (principal separation, fencing, explicit child environment, proxy scope, workspace ownership) with a pointer to `docs/release/security.md`; keep the pinned phrase "does not ship a repository command-line interface".
4. DOCS-001: correct the schema to the 12-position shape `verifyEvidence()` emits and fix the `$id` host; add a test in `scripts/templates-release-demo.test.mjs` that validates the demo's `verification.json` against the schema using structural checks (no new dependency).
5. DOCS-006: fix the "Disposition" and "Blind review" entries in `CONTEXT.md` to match the canonical templates in `agents/templates/`, and add one trigger-first pointer line to `AGENTS.md` "Everyone" per that file's editing rules so the glossary has a readership.

## Out of scope

- `docs/runbooks/quiet-window-auto-deploy.md` (owned by the predecessor chain) and any other runbook.
- Changing any routing rule, default route, or the parser; this is documentation of the existing behaviour.
- Deleting any of the five files.

## Constraints

- Every fact keeps exactly one owner; a pointer never restates the rule.
- No change may break `scripts/release-docs.test.mjs`, `scripts/operator-api-docs.test.mjs` or `npm run test:snapshot-scan`.

## Acceptance

- `grep -c "implementation=" docs/BRIEF-TEMPLATE.md docs/operator-api.md` shows the grammar stated once outside `task-routing-v1.md` at most as an example line.
- `grep -n "screenshots above" docs/architecture.md` returns nothing.
- The schema's `positions` constraint is 12 and the new demo test passes.
- `git grep -n "CONTEXT.md" AGENTS.md` returns one hit.
- `npm run test:release-docs`, `node --test scripts/operator-api-docs.test.mjs scripts/templates-release-demo.test.mjs`, `npm run test:snapshot-scan`, `npm run lint` pass.
<!-- /agentos:task-brief:v1 -->
Persist the final implementation output for this step through the Anneal task output endpoint.
<!-- /agentos:task-brief:v1 -->
Persist the final implementation output for this step through the Anneal task output endpoint.