Goal: a merge gate FAIL means the branch is wrong. On 2026-09-06 three separate tests failed only because the gate worker was loaded, each one stopping a delivery that was in fact correct, and each was proved a flake by re-running the identical tree on an idle worker.

Background: the merge gate is the only verdict the platform trusts, and it runs on a worker that also carries the host's Runs (VM load1 20-55 all day on 2026-09-06). Three failures that day were timing artefacts, not defects:
1. `packages/merge-executor/src/index.test.ts` "a mismatched daemon process stays alive and still exits 0 on SIGTERM" - a fixed 10 s readiness budget. It stopped PR #515 at 11:40Z and lane L6 at 12:2xZ. A later main commit widened it to 60 s with a private TMPDIR; that fix landed, so this instance is closed and is listed only as evidence of the shape.
2. `packages/api/src/control-plane-ownership.test.ts` "RP-OWN-FILES-ALIAS ..." and "RP-OWN-SAME-ALIAS ..." - `Timed out after 10000ms waiting for child exit` and `Timed out after 10000ms waiting for /OWNERSHIP_PROBE_READY/u`. They produced `MERGE GATE: FAIL (unit tests (all workspaces))` for lane L7 at 15:59Z on the VM worker (`/home/agentos/gate/agentos/logs/20260906T155930Z-b201fc5d...`), while the same tree gated `PASS` on the idle Tencent worker at 16:30Z. The same log carries `Anneal API 503: events temporarily unavailable` and `Run Lease renewal failed`, so the host was demonstrably saturated.
3. `apps/web/src/tests/sessions-filters.test.tsx:286` "Load more pages through the filtered history under the same filters" - `AssertionError: the second page carries the cursor` (`actual: null`). It asserts `listRequests(page).at(-1)` is the pagination request, so any request that lands after `page.settle()` returns makes the assertion read the wrong request. It failed the P0 deploy-deadlock chain's gate at 17:20Z (`20260906T172029Z-eb3193fc...`) and appears again in the 15:59Z log above. Adjacent VM gates that hour passed, so it is intermittent. **Update 18:45Z: this instance is already fixed on `main` by commit `eca81200` ("test(web): assert the paged Sessions request, not the newest one"), which selects the paged request among the requests issued after the press instead of taking the newest one. That commit was not yet on `main` at the 17:20Z gate, so the failure was real; it is now closed upstream. Verify it after merging `main` and do not redo it — it is listed here as evidence of the shape, like instance 1.**

Two distinct shapes are behind these: a fixed wall-clock budget for a child process or a readiness probe, and an assertion that reads the *last* observed request rather than the request it means.

Changes:
1. Survey the suites the gate runs for fixed wall-clock budgets that wait on a spawned child, a readiness probe, or a settle. Report the list. For each, replace the fixed budget with one that is generous on a loaded host, or with a wait on the actual condition rather than a duration. Do not change what any test asserts.
2. Fix instance 2 (`control-plane-ownership.test.ts`) concretely. Instance 3 needs no change: after merging `main`, confirm `eca81200` is present and that `sessions-filters.test.tsx` selects the paged request among those issued after the press; record that confirmation and move on. If the survey in Change 1 finds the same last-request shape elsewhere, fix those the way `eca81200` did.
3. Where a budget must stay bounded so a genuine hang is still caught, say so at the budget with the reason, and pick the bound from the observed loaded-host duration with margin, not from the idle-host duration.

Out of scope: gate worker capacity, runner counts, and any host sizing change - all three are Moson-owned and were ruled on 2026-09-06 (Mac Claude runners stay at 8, Claude never on the VM, VM gate worker-capacity stays 1). Retrying a failed gate automatically. Quarantining or skipping tests. The merge-executor readiness budget in instance 1, already fixed on main.

Constraints: no test is deleted, skipped, or weakened in what it asserts; a genuinely hanging child must still fail rather than hang forever; no new environment variable or configuration surface for test timing.

Acceptance: the survey from Change 1 is recorded in the implementation output with file and line for every fixed budget found. `npm run test` is green. The tests named in Change 2 pass when the host is loaded: demonstrate it by running the affected workspace's suite under `--test-concurrency` pressure, or by an equivalent stress the implementation names and records. The implementation output records that instance 3 was already closed by `eca81200`. `npm run lint` is clean.

Route: implementation=senior-dev-opus-medium - a bounded, well-evidenced test-timing change with two concrete instances and a survey; no product behaviour changes

Route: implementation=senior-dev-opus-medium - a bounded, well-evidenced test-timing change with two concrete instances and a survey; no product behaviour changes
