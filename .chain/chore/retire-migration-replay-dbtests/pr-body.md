Retire the five enumerated shipped one-shot migration replay suites while retaining their current-schema assertions. CONTRIBUTING.md states the first-release-after-shipment retention rule once. No Prisma migration, schema, operator CLI, or gate execution changes are included.

Per-file assertion map (paths below are relative to `packages/api/src/` unless stated otherwise):

| Deleted suite | Replay-only assertions retired | Current-schema assertions preserved and destination |
| --- | --- | --- |
| `native-implementation-subagents-upgrade.dbtest.ts` | Backfill assigns Luna/8 only to active canonical implementation Codex Runs, leaving failed, planning, and Claude Runs unchanged. | Both Run subagent columns exist and all six retired Agent/Run subprocess columns are absent: `migration.dbtest.ts`, “current Run subagent columns replace all retired Agent and Run subprocess columns”. |
| `optional-steps-migration.dbtest.ts` | Historical step backfill and before/after equality of Task approval gates, chain indexes, and chain layers across migration. | The surviving optional field has a NOT NULL false database default: `migration.dbtest.ts`, “current template steps have a non-null false optional database default”. The retired Project.skipOptionalSteps column is not a current invariant. |
| `prior-outputs-upgrade.dbtest.ts` | Pre-whitelist canonical inventory shape, migration backfill to the legacy sentinel, and historical migrate-then-sync sequence (including filling the then-missing PR template). | Legacy sentinel claim delivers every earlier kind/body in chain order and bypasses missing-kind refusal: `prior-outputs-whitelist.dbtest.ts`, “a legacy all-prior-outputs step claims every earlier output in chain order”. Explicit whitelist claim behavior remains in that file’s “a 12-step toy chain sends every claim exactly its declared prior outputs”. Canonical adoption of legacy declarations remains in `packages/db/src/canonical-step-adoption.test.ts`, “the retired all-output handoff marker is adopted wherever it survives”; real sync adoption and inventory installation remain in `packages/db/src/canonical-prompt-sync-multi-project.dbtest.ts`, “partial template rows synchronize across Projects, leave missing rows valid, and adopt transitions” and “full installation fills only the addressed Project's missing inventory and is idempotent”. |
| `project-gate-defaults-migration.dbtest.ts` | Existing project backfill and preservation of true/false Task approval values across the additive migration. | Fresh project spec/merge defaults remain false and patch independently: `project-gate-defaults.dbtest.ts`, “fresh project defaults are returned by the real API and patch independently”. |
| `goal-execution-upgrade.dbtest.ts` | Historical Task generation-zero/iteration/predecessor/dispatch-key/hash backfill; Run lineage inheritance; successful migration ordering; atomic rollback of schema, enum, and rows for ambiguous historical Runs. | Fully migrated lineage constraints are present and validated: `migration.dbtest.ts`, “goal 5a0 migration installs the exact enum labels, columns, checks, FKs, and indexes”, now asserting `convalidated` for every retained check. Live lineage write constraints remain in “goal 5a0 constraints reject every corrupt lineage shape and accept manual all-null rows” in the same file. |

`goal-execution-fixture.ts` stays because `preflight-goal-execution.dbtest.ts` and `preflight-goal-execution-abort.dbtest.ts` import it. Its unused kernel-application method and exposed schema/quoted members are removed; pre-kernel staging, execution, and cleanup remain for those operator preflight tests.

Scope correction from review: `agent-canonical-role-migration.dbtest.ts` and `staffing-profiles-migration.dbtest.ts` remain unchanged in behavior. They were not in the enumerated retirement list, and this task does not establish that their `20260905120000_staffing_profiles` migration has shipped. Their original `targetMigration` identifiers are restored. The acceptance section in `spec.md` now expects exactly these two named results, plus absence of the five retired suites and the dead kernel-application method; the previous identifier rename did not prove retirement. This is a scope exemption, not a claim that all replay suites in the repository are gone or evidence of installation release status.

Validation: API workspace lint and typecheck passed; API unit tests passed (975 passed, 0 failed, 3 skipped). Static scope checks and `git diff --check` passed. Whole-repository lint/typecheck and all database regressions are owned by the chain’s Regression merge gate, per AGENTS.md. Affected database coverage is `migration.dbtest.ts`, `prior-outputs-whitelist.dbtest.ts`, both preflight Goal tests, and the two retained staffing/canonical-role migration suites. The platform owns PR creation and should use this per-file account in the PR body.
