Review outputs: retire the temporary sol-findings execution compatibility

Goal: no live execution path recognizes the `sol-findings` output kind; historical Task, Run, output and report rows that carry it stay readable; template rollover and upgrade readers that still translate it stay.

Background: PR #574 (model-neutral review outputs) made canonical Chains produce `review-findings` and kept `sol-findings` as a transitional execution alias for Chains instantiated before it. The operator ruled the retirement boundary on 2026-09-08 after a read-only inventory: 280 Chains carry a `sol-findings` Step; every Chain that was unarchived and unfinished (three word-factory Chains 3cd7177f, 21cc2d0b, bb60e911) was archived on 2026-09-08 and re-created on current templates, so no unarchived Chain still needs execution, retry or resume under the old contract; the 103 archived-and-unfinished Chains are abandoned and keep no supported retry or resume path; archived done Chains stay readable. Live compatibility that this card retires, at current HEAD: the step-role alias map `packages/db/src/step-role.ts:33`; the repair evidence kind list `packages/db/src/run-output-evidence.ts:88`; the prior-output whitelist `packages/api/src/merge-tail-actions.ts:990`; the PR handoff wire acceptance `packages/runner/src/delivery.ts:200` (`PR_LEGACY_REVIEW_FINDINGS_KIND`); and the staffing-profile carry that maps the old kind (`packages/db/src/staffing-profile-carry.test.ts` pins it). Rollover and upgrade readers that stay: `canonical-template-installation.ts:291` (`{ "sol-findings": "review-findings" }` adoption) and the generation fixtures in `canonical-template-transition.ts`.

Changes:
1. Remove `sol-findings` from step-role recognition, the repair evidence kind list, the merge-tail prior-output whitelist, the runner PR handoff wire acceptance, and staffing-profile carry. A Step or Run that presents `sol-findings` on any of these paths is refused loudly with a named reason (the existing unknown-kind refusal of each path), never mapped.
2. Keep the canonical template installation adoption map and the transition generation fixtures unchanged, so an installation whose templates still predate #574 upgrades exactly as today.
3. Historical rows are neither rewritten nor deleted: `GET /tasks/:id`, outputs, and report text for a Chain with `sol-findings` Steps still return them verbatim.
4. Delete the tests that exercise the retired execution alias (in `step-role.test.ts`, `run-output-evidence.test.ts`, `staffing-profile-carry.test.ts`, `delivery.test.ts`, `mcp-server.test.ts`, `control-plane.test.ts`, `canonical-task-output.test.ts`, `session.test.ts`, `specification-fidelity.test.ts`, `merge-integrator-seed.dbtest.ts`); keep and, where missing, add the tests for historical read access and for the fail-loud refusal; keep the transition and prompt-sync tests untouched.
5. `agents/README.md` and `docs/operator-api.md` no longer describe `sol-findings` as an accepted execution kind; `CHANGELOG.md` records the retirement and the boundary above.

Out of scope: rerunning or rewriting any existing review; deleting historical data, templates, or the legacy template generations; changing Agent model defaults; the canonical template transition machinery itself; the three re-created word-factory Chains.

Constraints: no live database mutation and no migration; no scheduled deletion or automatic cutoff; the merge gate, merge readiness, and lease behavior are unchanged for `review-findings`.

Acceptance: `grep -rn "sol-findings" packages --include=*.ts` matches only `canonical-template-installation.ts`, `canonical-template-transition.ts`, `canonical-prompt-sync-fixtures.ts`, and their tests; a unit test per retired path shows the old kind refused with a named reason; a dbtest reads an archived Chain seeded with a `sol-findings` Step and its report verbatim; `npm run lint`, the API, db and runner unit suites, and `npm run test:snapshot-scan` pass.

Route: implementation=senior-dev-astra-medium - hazard tier (alters repair evidence selection, merge-tail prior-output acceptance and the PR handoff wire contract; a wrong refusal strands a live merge tail)
